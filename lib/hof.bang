
fun :foldl operator = {
  # 1 > ?
  operator! operator foldl!
}

fun :map xform = {
  fun :innermap = {
    # 0 > ?
    as val innermap! val xform!
  }
  innermap!
}

fun :filter predicate = {
    fun v = {
      v predicate! ? v
    } map!
}

fun = lookup
