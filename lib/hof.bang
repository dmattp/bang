
def :map xform = {
  def :innermap = {
    fun val = innermap! val xform!;
    # 1 > ?
  }
  innermap!
}

--def :filter predicate = {
--    def :filter-xform v = {
--      fun = v; v predicate!?
--    }
--    filter-xform map!
--}


def :filter predicate = {
    fun v = {
      fun = v; v predicate!?
    } map!
}

fun = lookup
