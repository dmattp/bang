
def :foldl operator = {
  # 1 > ?
  operator! operator foldl!
}

def :map xform = {
  def :innermap = {
    # 0 > ?
    as val innermap! val xform!
  }
  innermap!
}

def :filter predicate = {
    fun v = {
      v predicate! ? v
    } map!
}

fun = lookup
