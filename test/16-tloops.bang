
def :times totalTimes = { fun! loopFun =
    def :innerLoop remain = {
      loopFun!
      remain 1 >
      ? remain 1 - innerLoop!
    }
    totalTimes innerLoop!
}

def :say-hello = "hello\n" print!;

say-hello 7 times!
