

def :map xform = {
  def :innermap = {
    fun val = innermap! val xform!;
    # 1 > ?
  }
  innermap!
}

def :Zilter predicate = {
    def :Zilter-xform v = {
      fun = v; v predicate!?
    }
    Zilter-xform map!
}

def :more-than-seven v =  v 7 >;

   5 3 15 7 4 12 8
more-than-seven Zilter!
